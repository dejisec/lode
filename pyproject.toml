[project]
name = "lode"
version = "0.1.0"
description = "Multi-agent research system"
requires-python = "==3.12.*"
dependencies = [
    "openai-agents>=0.0.3",
    "pydantic>=2.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

